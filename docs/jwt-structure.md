# JWT Structure

This document describes the claims included in the JWT token.
Claims included in token:
- sub  → UserId (Subject)
- email → User Email
- role → User Role
- jti  → JWT Unique Identifier
- sid  → Session Identifier
- exp  → Expiration Time
